CREATE DATABASE portfolio;

USE portfolio;

CREATE TABLE sections (
    id INT AUTO_INCREMENT PRIMARY KEY,
    section_name VARCHAR(255) NOT NULL UNIQUE,
    content TEXT NOT NULL
);

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(255) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);
CREATE TABLE admins (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(255) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);
--when u insert make sure u seclect the write tabel
-- Add an admin user
INSERT INTO admins (username, password) 
VALUES ('admin', PASSWORD('password'));
CREATE TABLE admins (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(255) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL
);-- Insert an admin user
INSERT INTO admins (username, password) 
VALUES ('admin', '$2y$10$ZqP3C1gBsDP7L1MxYTVCKeJTXKP9JlNFS7aEOYlK5.9rfNpZnYDbG'); -- Password: password

INSERT INTO sections (section_name, content) VALUES
('About', 'Content for the About section.'),
('Services', 'Content for the Services section.'),
('Portfolio', 'Content for the Portfolio section.'),
('Testimonials', 'Content for the Testimonials section.'),
('Contact', 'Content for the Contact section.');
